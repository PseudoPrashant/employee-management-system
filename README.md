# employee-management-system
Built a PHP + MySQL Employee Management System with admin login (sessions) and full CRUD operations. Implemented employee search and department filtering for fast record management. Used optimized SQL queries and clean backend logic with structured code and responsive UI using HTML/CSS.
